apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: tetris
  namespace: flux-system
spec:
  interval: 5m
  prune: true
  sourceRef:
    kind: GitRepository
    name: kustomizations
  path: ./kustomizations/service
  postBuild:
    substitute:
      name: tetris
      image: tetris-server
      tag: master-b784c7f7-1641777966 # {"$imagepolicy": "flux-system:tetris:tag"}
