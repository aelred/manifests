apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: skakoui
  namespace: flux-system
spec:
  interval: 5m
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./kustomizations
  postBuild:
    substitute:
      name: skakoui
      tag: master-40772ec2-1641772408 # {"$imagepolicy": "flux-system:skakoui:tag"}