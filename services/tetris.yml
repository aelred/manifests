apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: tetris
  namespace: flux-system
spec:
  interval: 5m
  prune: true
  sourceRef:
    kind: GitRepository
    name: kustomizations
  path: ./kustomizations/service
  postBuild:
    substitute:
      name: tetris
      image: tetris-server
      tag: master-74a463ae-1698870847 # {"$imagepolicy": "flux-system:tetris:tag"}
